#ifndef FILE_UTILS_H
#define FILE_UTILS_H

#include <string>

// Function to save the hashed message to the user's Documents directory
void saveHashedMessageToFile(const std::string& hashedMessage);

#endif // FILE_UTILS_H
